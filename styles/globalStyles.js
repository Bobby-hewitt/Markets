import {
  StyleSheet,
} from 'react-native';

export default styles = StyleSheet.create({
  center:{
    justifyContent:'center',
    alignItems:'center'
  },
  fullRow:{
    flexDirection: 'row',
    flex:1
  }
});